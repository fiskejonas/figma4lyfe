---
const { titel, deluxe, variant, knapTekst } = Astro.props;
---

<button data-variant={variant} class="bg-slate-200 text-cardButtonMobile p-2 rounded"
  >{knapTekst}</button
>

<style>
  [data-variant="primary"] {
    font-size: 10px;
  }

  [data-variant="secondary"] {
    font-size: 10px;
  }
</style>